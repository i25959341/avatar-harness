# Sample Media

`idle.mp4` is the default moving idle loop used by the FlashHead adapter.
`imtalker_idle_driver.mp4` is the aligned driver used to build an IMTalker idle cache.

Replace these files through the backend environment variables or command-line options
when using another identity. A source portrait, idle media, and generated idle cache
must use compatible identity, crop, and framing.
